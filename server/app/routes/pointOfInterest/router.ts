import { Context, Router } from "oak";

const poiRouter = new Router();

poiRouter.post('/', (ctx: Context) => {
    
});

poiRouter.get('/for-user/:id', (ctx: Context) => {
    
});

export default poiRouter;
