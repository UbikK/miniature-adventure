{
  "packageManager": "yarn@3.2.3",
  "name": "@miniature_adventure/domain",
  "private": true,
  "version": "1.0.0",
  "main": "src/mod.ts",
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "postinstall": "yarn build",
  "watch": "tsc --watch",
  "dependencies": {
    "rimraf": "^4.1.2"
  }
}
